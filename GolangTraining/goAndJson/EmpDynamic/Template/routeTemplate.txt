package {{.Info.Title}}files

import(
	"fmt"
	"net/http"
	"github.com/gorilla/mux"
)

func MuxRoutes(){
	
	r := mux.NewRouter()
    {{ range .Funcs }}
	r.HandleFunc("{{.Path}}", {{.OpID}}).Methods("{{.Method}}")
    {{end}}
	
	fmt.Println("Server started")

	{{range .Server}}
	http.ListenAndServe(":{{.Port}}", r)
	{{end}}
}